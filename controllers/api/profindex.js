/* const router = require('express').Router();
const { appendFile } = require('fs');

const profileController = require('./profileRoutes');


router.use('/profile', profileController);

module.exports = router; */